import { gameFrame } from "../main.js";
import FlyingCoin from "../entity/flyingCoin.js";

const flyingCoins = [];

const handleFlyingCoins = () => {
/*   if (gameFrame % 100 === 0) {
      flyingCoins.push(new FlyingCoin);
  }
  flyingCoins.forEach((flyingCoin) => {
    flyingCoin.update();
    flyingCoin.draw();
  }); */
};

export { flyingCoins };
export default handleFlyingCoins;
